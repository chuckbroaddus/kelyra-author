# Role name matches .grok/personas/author-qa.toml so workflow
# agent_type "author-qa" applies the persona, model, and effort.
description = "Kelyra Author read-only QA reviewer using Grok 4.5"
default_capability_mode = "read-only"
model = "grok-4.5"
reasoning_effort = "high"
